// import { debounce } from "../utils/debounce"

const analyticsEvents = {
    // linkClick: debounce((value) => {
    //     window.dataLayer.push({ event: "linkClick", value })
    // }, 300),
}

export default analyticsEvents;
